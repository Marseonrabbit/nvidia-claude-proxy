# NVIDIA API Configuration
NVIDIA_API_KEY=your_nvidia_api_key_here
NVIDIA_API_URL=https://integrate.api.nvidia.com/v1

# Optional: Authentication token for your proxy
# If set, all requests must include either:
#   Authorization: Bearer <AUTH_TOKEN>
#   x-api-key: <AUTH_TOKEN>
AUTH_TOKEN=your_optional_proxy_token_here

# Cloudflare configuration (automatically handled by wrangler)
# Secrets are stored in Cloudflare's secret manager, not in this file
